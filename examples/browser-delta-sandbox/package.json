{
  "name": "@axon/browser-delta-sandbox",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build:wasm": "cargo build -p browser-delta-sandbox --target wasm32-unknown-unknown --release --locked && wasm-bindgen ../../target/wasm32-unknown-unknown/release/browser_delta_sandbox.wasm --target web --out-dir src/wasm --typescript",
    "dev": "vite --host 127.0.0.1",
    "build": "npm run build:wasm && tsc --noEmit && vite build",
    "test:e2e": "npm run build:wasm && playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.57.0",
    "@types/node": "^22.18.0",
    "@vitejs/plugin-basic-ssl": "2.1.0",
    "typescript": "5.9.3",
    "vite": "^7.3.2"
  }
}
